{
  "name": "npm-trustme",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "npm-trustme": "dist/main.js"
  },
  "scripts": {
    "dev": "tsx src/cli/main.ts",
    "build": "tsup src/cli/main.ts --format esm --dts --minify --out-dir dist",
    "lint": "echo \"(no lint configured)\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "citty": "^0.1.6",
    "dotenv": "^16.4.5",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsup": "^8.3.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18"
  }
}
